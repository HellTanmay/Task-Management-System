# Task Managment System

## Installation
Install php and javascript dependencies
```bash
composer install
npm install
```
Generate an application key using the below command

```bash
php artisan key:generate
```           
Start the laravel server

```bash
php artisan serve
```
Open another terminal and run the node server

```bash
npm run dev
```
